# EmberlightWandere · 项目根指令

本文件是本仓库的 AI 协作规则入口。**改代码或文档前先读本文件**，再按下面的路由进入对应文档。

## 工程事实

| 项 | 值 |
|----|----|
| Unity | **2022.3.62f3** |
| 主场景 | `Assets/Scenes/Emberlight.unity`（唯一启用场景） |
| 命名空间 | `Emberlight`；编辑器为 `Emberlight.Editor` |
| 脚本根 | `Assets/Scripts/{Core,Combat,UI,Visuals,Audio}` + `Assets/Editor` |
| 文档根 | `Docs/` |
| 程序集 | **无 `.asmdef`** → 全部进 `Assembly-CSharp` / `Assembly-CSharp-Editor` |
| 自动化测试 | **不存在**。当前验证手段只有编辑器编译 + 手动试玩 |
| GitHub | `https://github.com/gtxy688/EmberlightWandere.git` |

> ⚠️ 旧路径 `Assets/Emberlight/Scripts/...` **已废弃**，以 `Assets/Scripts/...` 为准。

## 文档路由

文档分四层，各有明确权威范围：

| 文档 | 回答 | 权威范围 |
|------|------|----------|
| `Docs/requirements.md` | 做什么、为什么、约束与数值 | **产品行为与验收目标** |
| `Docs/architecture/00-overview.md` | 有哪些模块、依赖与任务路由 | **模块边界与唯一任务索引** |
| `Docs/architecture/0X-*.md` | 如何实现、公开契约和影响范围 | **当前模块设计** |
| `Docs/architecture/0X-*-test.md` | 如何自动验证、如何手动验收 | **测试与验收证据** |
| `Docs/progress.md` | 当前进度与阻塞 | **状态记录，不定义需求或架构** |
| `Docs/reference/*` | 素材来源与授权 | 参考资料 |
| `Docs/archive/*` | 历史决策与踩坑记录 | **只读历史，不要从归档实现功能** |

### 新任务怎么开始

1. 打开 `Docs/architecture/00-overview.md` 的**任务索引**，用关键词定位主要模块。
2. 声明准备读取的最小文档集并继续；只有映射不唯一、缺少权威信息或影响范围不清楚时才等 jack 裁定。
3. 最小文档集通常包括：当前模块架构、它引用的 `requirements.md` 章节、对应验收文档。
4. 存在显式依赖或回归影响时，只补读相关模块的「对外契约」或验收文档的「回归范围」。

> 不要因为"可能有用"而通读整个 `Docs/`，也不要因为"禁止跨模块"而拒绝读取已声明依赖的契约。

## 冲突处理（重要）

本仓库历史上积累了 40+ 篇平摊文档，**已发现的数值冲突全部集中在 `Docs/archive/`**。规则：

1. **`requirements.md` 是数值唯一权威**。架构文档不复制数值；发现不一致以 `requirements.md` 为准。
2. 归档文档**只作历史**。引用归档里的数值当作现行口径是错误做法。
3. 发现不一致时：**暂停受影响的改动** → 列出双方位置与差异 → 说明裁定影响 → 拿到裁定后再改 → 同步更新权威文档与验收证据。**不要静默选边。**
4. 代码与测试是**当前实现证据**，不会自动覆盖文档中的意图；反之文档也不等于代码现状。二者不符时同样走上面的流程。

### 已知冲突（已裁定，勿再翻案）

| 项 | 归档里的旧说法 | 现行权威值 |
|----|----------------|------------|
| 怪物血量倍率 | `0.75 / 1 / 1.35`；或休闲 `0.5` | **`0.25 / 0.5 / 0.675`** |
| 幸运品质权重 | `60/28/10/2`（幸运 0） | **`72/22/5/1`（幸运 0）** |
| 攻速档位 | 与攻击力同档 `+10/20/30/40` | **`+10/20/30/50`**，且**只作用于单武器** |
| 攻击力作用域 | 加算全局 | **只作用于对应已有武器** |
| Boss 触发 | `Elapsed >= 480` | **波次驱动**（每 10 波 + 最终波） |
| 局长 | 6 波（Wave6 Boss） | **25 / 50 波** |
| 粒子上限 | 180 | **120** |
| 护盾数值 | 除以 2 | **250 / 500 / 750 / 1250** |
| 死亡掉落 | 掉落余烬拾取物 | **已删除**，击杀直接记 Score |

## 硬约束（违反即事故）

1. **中文字体已改为预烘静态图集，改动前必读 `Docs/architecture/07-art-presentation.md`**。历史上两次预烘都导致 UI 全崩、游戏进不去，而资产字段校验全绿；两次根因已定位并修正（写入顺序 + 真实渲染校验）。**不要删掉 `EmberFonts.ValidateBaked` 那道运行时闸门**，也不要绕过它直接把未校验的字体资产塞给运行时——闸门是"资产坏了就回落动态图集"的唯一保证。**静态图集不能追加字形，改任何文案后必须重跑烘制**：
   ```
   python Tools/atlas_glyph_check.py      # 先查缺口，退出码非 0 表示有字没烘进去
   Unity.exe -batchmode -quit -projectPath <项目> -executeMethod Emberlight.Editor.EmberFontBake.Bake
   ```
   该脚本直接读编译后的 `Assembly-CSharp.dll` 字符串堆，能看见**方法体内的内联字面量**，因此是"文案全在盘上"的唯一可靠判断依据。它还会**显式检查 TMP 的两个特殊字符** `U+005F`（下划线）与 `U+2026`（省略号）——TMP 每次给 `TextMeshProUGUI` 赋字体都会去字体里找这两个，缺一个就每个 label 报一条 `The character used for Underline is not available` 警告。`U+005F` 是 ASCII，看着"显然存在"，但**没有任何游戏文案含它**，手写白名单和字面量扫描都抓不到，所以靠脚本兜。**批处理必须先关 Unity**，否则 `HandleProjectAlreadyOpenInAnotherInstance` 直接 crash。
2. **不要引入全局攻击力**，也不要把攻击力/攻速改回全局作用域——会让早期伤害膨胀。
3. **不要恢复局外成长、XP / 经验条、`Elapsed >= 480` Boss 触发、ScoreMult 随 N 变化**。见 `requirements.md` §6。
4. **开局不可跳过**：必须选 N 并单选 1 把武器；**禁止开局一次填满 N 槽**。
5. **不要在 `Assets/Scripts/Combat/Weapons/` 里引用 `EmberGame`**。武器只能通过 `EmberWeaponContext` 与 `RunProgress` 工作——这是本仓库最干净的边界。
6. **不要在 `Assets/Scripts/UI/` 里引用 `EmberGame`**。UI 通过方法参数接收只读数据。
7. **四个图集 `.meta` 现在是真正的 sprite sheet，切片权威在那里**。四个 `AssetPostprocessor` 导入器（`Ember*ArtImporter`）负责把 `textureType` 设为 `Sprite`、`spriteImportMode` 设为 `Multiple`，**不要让它们改回 `Default`**——历史上它们强制 `Default`，导致每次重新导入都把切片清掉、UI 退回运行时切图。切图本身用 Sprite Editor 做，格子与命名保持「整格均分 + `<atlas>_<index>`（行主序，左上起）」。
8. **新增音频后必须检查导入设置**为 `3D: 0` + `preloadAudioData: 1`（新 WAV 默认是 3D + 不预载，表现为"资源在但听不到"）。
9. **UI 与字体改动必须在编辑器里真进一局确认**。资产校验替代不了真跑一遍。

## 改动约定

1. **先读再改**：动战斗节奏前读 `EmberCombat` / `EmberBoss` / `LevelConfig`；动选卡前读 `RunProgress` / `EmberRarity`；动 UI 前读 `EmberMenuUi` / `EmberHud` / `EmberUpgradePanel`。
2. **数值集中在配置类**：新增可调数值应进 `LevelConfig`（或对应模块的配置归属），不要在战斗逻辑里写魔法数。
3. **小步可回退**：一次只改一个闭环问题。
4. **同步文档**：改了公开行为或数值，必须同一批改动内更新 `requirements.md` 对应章节 + 该模块架构的「对外契约/影响范围」+ 验收文档的「回归范围」。
5. **数值与规则**以 `requirements.md` 与 xiaoPlanner 规格为准。
6. **安卓真机排最后**：未完成游戏闭环前不主动扩 APK / 真机专项。
7. **不要派工风暴**：除 jack 明确要求，Agent 之间不互相催办、不群发任务；需要协作时回 jack 点名。

## 验收与交付口径

交付时必须区分四类，不允许含糊：

| 类别 | 定义 |
|------|------|
| **已验证** | 实际运行且通过的检查（附命令与结果） |
| **不适用** | 经说明理由后无需执行的检查（记 `N/A` + 理由，**不算未验证**） |
| **待人工验收** | 手感、审美、可读性、真机表现等必须由人在设备上判断 |
| **未验证** | 因环境、资源或权限无法执行的检查 |

- **当前项目没有任何自动化测试**。因此：
  - **禁止**再写「`Emberlight/Validate …` 已通过」「`ValidateGodsSelect` 覆盖验收点」这类表述——这些入口**已被删除**。
  - 需要自动化但尚无基础设施的检查，在验收文档中标为**待建**并写明拟测层级。
  - 只有所有适用自动化检查通过**且**适用的人工验收完成后，模块才可标记为**已验收**。
- AI 可以报告"代码实现完成"与"自动化验证完成"，但**不能代替 jack 做手感与审美验收**。
- 任何改动 UI 或字体的提交，都需要在编辑器里手动进一局确认。

## 自检（改完至少做）

- [ ] Unity 编译无报错；Console 进主菜单无错误
- [ ] 能开一局：选长度 → 选难度 → 选槽位 → 选武器 → 升级 → 暂停 → 胜或负
- [ ] 若动 Boss / 刷怪：波次推进与 Boss 出场符合 `requirements.md` §3
- [ ] 若动选卡：候选数量、同屏唯一、补偿选卡、刷新次数符合 `requirements.md` §4
- [ ] 若动 UI / 字体：在编辑器里真进一局，中文无方框、Console 干净
- [ ] 已回写受影响的文档章节

## 角色

| 角色 | 负责 | 不负责 |
|------|------|--------|
| jack | 拍板、试玩反馈、优先级、手感与审美验收 | — |
| xiaoProducer | 拆计划、标依赖/风险、指该找谁 | 不大段写 C#；不定数值终稿；不主动派工 |
| xiaoPlanner | 玩法/数值/波次/验收规格 | 不写大段引擎代码 |
| xiaoCoder | C#、系统、性能、可运行改动 | 不定玩法终稿 |
| xiaoLevel | 关卡节奏、遭遇、场景 checklist | 不大改核心玩法代码 |
| xiaoUI | HUD/菜单/交互说明 | 不定数值 |

未指派时：文档归 Producer/Planner；代码归 Coder；关卡表归 Level；界面归 UI。

## 文档健康度（每次交付前检查）

- [ ] 模块文档引用的 `requirements.md` 章节仍然存在
- [ ] 「对外契约」与实现一致
- [ ] 验收文档覆盖本次行为变化及回归影响
- [ ] 新任务关键词已加入 `00-overview.md` 的**唯一**任务索引
- [ ] 数值没有复制到多个权威位置（只在 `requirements.md` 有完整表）
- [ ] `progress.md` 没有被当成需求来源
- [ ] 归档文档没有被当作现行口径引用
